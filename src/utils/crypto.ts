// import { randomBytes } from "node:crypto";

// export const genSalt = function genSalt() {
//     randomBytes(16, function(error, randomBytes: randomBytes: Buffer) {
//         if (error) {
//             cb(error);
//             return;
//         }

//         gen_salt('b', 12, randomBytes, cb);
//     });
// }